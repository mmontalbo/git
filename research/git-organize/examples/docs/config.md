area: reference

# Configuration reference

Every setting, its default, and its meaning. The
[command reference](commands.md) says which command reads each one.

New here? Start with the [install guide](install.md).
